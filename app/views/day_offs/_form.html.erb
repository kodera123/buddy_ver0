<%= form_for(day_off) do |f| %>
  <% if day_off.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day_off.errors.count, "error") %> prohibited this day_off from being saved:</h2>

      <ul>
      <% day_off.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :off_plan %>
    <%= f.date_select :off_plan %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
